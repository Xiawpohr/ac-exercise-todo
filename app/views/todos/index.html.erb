<h1>My TODO List</h1>
<ul>
	<% @todos.each do |todo| %>
		<li>
			<%= todo.task %>
			<%= todo.due_date %>
			<%= link_to 'Update', edit_todo_path(todo) %>
			<%= link_to 'Finished', todo_path(todo), method: :delete  %>
		</li>
		<% end %>
</ul>

<%= link_to 'New Task', new_todo_path %>