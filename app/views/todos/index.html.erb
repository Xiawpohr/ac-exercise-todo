<h1>My TODO List</h1>
<ul>
	<% @todos.each do |todo| %>
		<li>
			<%= todo.task %>
			<%= todo.due_date %>
			<%= link_to 'Edit', edit_todo_path(todo) %>
		</li>
		<% end %>
</ul>

<%= link_to 'New Task', new_todo_path %>